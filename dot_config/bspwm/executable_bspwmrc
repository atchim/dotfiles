#!/bin/sh

# Source every fragment in `conf.d` in lexical order.
for f in "${XDG_CONFIG_HOME:-$HOME/.config}"/bspwm/conf.d/*.sh; do
  [ -r "$f" ] || continue
  # shellcheck source=/dev/null
  . "$f"
done
